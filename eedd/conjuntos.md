# Conjuntos

## Declarar Conjuntos
```python
  cto = set()
  cto = 
```
Los elementos pueden ser de cualquier tipo:
```python
  cto = set()
  for i in range(1, 5)
    obj = Objeto()
    cto.append(obj)
```
